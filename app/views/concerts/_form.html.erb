<%= form_with(model: concert) do |form| %>
  <% if concert.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(concert.errors.count, "error") %> prohibited this concert from being saved:</h2>

      <ul>
        <% concert.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :assistants, style: "display: block" %>
    <%= form.number_field :assistants %>
  </div>

  <div class="field">
	    <%= form.label :group_id %>
	    <%= form.select :group_id, Group.all.map{|group| [group.name, group.id]} %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
